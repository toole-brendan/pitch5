'use client';

import React from 'react';
import { motion } from 'framer-motion';
import Slide from '@/components/core/Slide';
import { Box, Typography, Button, useTheme } from '@mui/material';

// Animation variants
const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.1
    }
  }
};

const phoneVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.6,
      ease: "easeOut"
    }
  }
};

const itemVariants = {
  hidden: { opacity: 0, y: 10 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.4,
      ease: "easeOut"
    }
  }
};

// Motion components
const MotionBox = motion(Box);

const DefenseUserExperience: React.FC = () => {
  const theme = useTheme();

  return (
    <Slide title="Defense User Experience">
      <Box sx={{ 
        display: 'flex', 
        height: '100%', 
        gap: 4,
        flexDirection: {
          xs: 'column',
          md: 'row'
        }
      }}>
        {/* Left Column: Phone Mockup */}
        <Box sx={{ 
          display: 'flex', 
          flexDirection: 'column',
          width: {
            xs: '100%',
            md: '40%'
          }
        }}>
          <Typography variant="h3" sx={{
            fontSize: '1.25rem',
            fontWeight: 600,
            color: theme.palette.text.primary,
            mb: 1,
            fontFamily: "'IBM Plex Mono', monospace",
            textTransform: 'uppercase',
            letterSpacing: '1px'
          }}>
            MOBILE INTERFACE_
          </Typography>
          
          <MotionBox 
            variants={phoneVariants}
            sx={{
              position: 'relative',
              width: '100%',
              height: '100%'
            }}
          >
            <Box sx={{
              width: '100%',
              bgcolor: theme.customColors?.phoneBorder || '#c4c9d4',
              borderRadius: '2rem',
              padding: '0.75rem',
              boxShadow: '0 10px 25px rgba(0, 0, 0, 0.15)',
              border: `1px solid ${theme.customColors?.militaryAccent || '#0066b3'}`
            }}>
              <Box sx={{
                bgcolor: theme.customColors?.phoneBg || '#f0f2f5',
                borderRadius: '1.5rem',
                overflow: 'hidden',
                position: 'relative'
              }}>
                <Box sx={{
                  bgcolor: theme.customColors?.militaryAccent || theme.palette.info.main,
                  color: 'white',
                  p: 2,
                  display: 'flex',
                  flexDirection: 'column'
                }}>
                  <Box>
                    <Typography variant="h6" sx={{ 
                      fontFamily: "'IBM Plex Mono', monospace",
                      fontSize: '1rem',
                      fontWeight: 600
                    }}>
                      HANDRECEIPT_
                    </Typography>
                    <Typography variant="subtitle2" sx={{ 
                      fontSize: '0.75rem',
                      opacity: 0.8
                    }}>
                      DEFENSE
                    </Typography>
                  </Box>
                </Box>
                
                <Box sx={{ p: 2 }}>
                  <Typography variant="h6" sx={{ 
                    fontSize: '0.9rem',
                    fontWeight: 600,
                    mb: 2
                  }}>
                    CURRENT INVENTORY
                  </Typography>
                  
                  <MotionBox variants={containerVariants} initial="hidden" animate="visible">
                    {[1, 2, 3, 4].map((item) => (
                      <MotionBox 
                        key={item}
                        variants={itemVariants}
                        sx={{
                          display: 'flex',
                          alignItems: 'center',
                          borderBottom: `1px solid ${theme.palette.divider}`,
                          py: 1.5
                        }}
                      >
                        <Box sx={{ 
                          width: '3rem',
                          height: '3rem',
                          bgcolor: '#e6eff8',
                          borderRadius: '0.5rem',
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          mr: 2
                        }}>
                          <Typography sx={{ 
                            color: theme.customColors?.militaryAccent || theme.palette.info.main,
                            fontWeight: 600,
                            fontSize: '0.8rem',
                            fontFamily: "'IBM Plex Mono', monospace"
                          }}>
                            {`MIL-${1000 + item}`}
                          </Typography>
                        </Box>
                        
                        <Box sx={{ flex: 1 }}>
                          <Typography sx={{ 
                            fontWeight: 600,
                            fontSize: '0.85rem'
                          }}>
                            {item === 1 ? 'Radio Component' : 
                             item === 2 ? 'Weapon System Part' : 
                             item === 3 ? 'Aircraft Component' : 
                             'Tactical Gear'}
                          </Typography>
                          
                          <Typography variant="body2" sx={{ 
                            fontSize: '0.75rem',
                            color: theme.palette.text.secondary
                          }}>
                            Qty: {item * 5} • Status: {item % 2 === 0 ? 'Deployed' : 'In Stock'}
                          </Typography>
                        </Box>
                        
                        <Box sx={{ 
                          display: 'flex',
                          alignItems: 'center',
                          justifyContent: 'center',
                          width: '2rem',
                          height: '2rem',
                          bgcolor: theme.customColors?.militaryBg || '#e6f0fa',
                          borderRadius: '50%',
                          color: theme.customColors?.militaryAccent || theme.palette.info.main
                        }}>
                          <Typography variant="body2">→</Typography>
                        </Box>
                      </MotionBox>
                    ))}
                  </MotionBox>
                  
                  <Box sx={{ 
                    display: 'flex',
                    justifyContent: 'center',
                    mt: 3 
                  }}>
                    <Button
                      variant="contained"
                      sx={{
                        bgcolor: theme.customColors?.militaryAccent || theme.palette.info.main,
                        color: 'white',
                        fontFamily: "'IBM Plex Mono', monospace",
                        textTransform: 'uppercase',
                        fontSize: '0.75rem',
                        py: 1,
                        px: 3,
                        borderRadius: '0.5rem',
                        boxShadow: 'none',
                        '&:hover': {
                          bgcolor: theme.customColors?.militaryAccent || theme.palette.info.dark,
                          boxShadow: 'none'
                        }
                      }}
                    >
                      SCAN NEW ITEM
                    </Button>
                  </Box>
                </Box>
              </Box>
            </Box>
          </MotionBox>
        </Box>
        
        {/* Right Column: Features */}
        <Box sx={{ 
          display: 'flex', 
          flexDirection: 'column',
          width: {
            xs: '100%',
            md: '60%'
          }
        }}>
          <Typography variant="h3" sx={{
            fontSize: '1.25rem',
            fontWeight: 600,
            color: theme.palette.text.primary,
            mb: 1,
            fontFamily: "'IBM Plex Mono', monospace",
            textTransform: 'uppercase',
            letterSpacing: '1px'
          }}>
            DEFENSE-GRADE FEATURES_
          </Typography>
          
          <Typography variant="body1" sx={{ 
            color: theme.palette.text.secondary,
            mb: 3
          }}>
            Specialized tools for military logistics and equipment management
          </Typography>
          
          <MotionBox 
            variants={containerVariants} 
            initial="hidden" 
            animate="visible"
            sx={{
              display: 'grid',
              gridTemplateColumns: {
                xs: '1fr',
                sm: 'repeat(2, 1fr)'
              },
              gap: 2
            }}
          >
            {[
              {
                title: 'SECURE AUTHENTICATION',
                description: 'Multi-factor authentication with biometrics and CAC integration for user verification',
                icon: '🔐'
              },
              {
                title: 'OFFLINE CAPABILITY',
                description: 'Full functionality in disconnected environments with secure sync when reconnected',
                icon: '📶'
              },
              {
                title: 'ITEM CUSTODY CHAIN',
                description: 'Complete history of all equipment transfers with digital signatures',
                icon: '📱'
              },
              {
                title: 'TACTICAL DEPLOYMENT',
                description: 'Field-ready interface optimized for quick updates in operational environments',
                icon: '🛡️'
              },
              {
                title: 'ENCRYPTED DATA',
                description: 'AES-256 encryption for all data at rest and in transit with FIPS compliance',
                icon: '🔒'
              },
              {
                title: 'MISSION READINESS',
                description: 'Real-time equipment status reports with critical alerts for component issues',
                icon: '🚨'
              }
            ].map((feature, index) => (
              <MotionBox
                key={index}
                variants={itemVariants}
                sx={{
                  p: 2,
                  bgcolor: theme.customColors?.militaryBg || '#e6f0fa',
                  borderRadius: '0.5rem',
                  border: `1px solid ${theme.customColors?.militaryAccent || theme.palette.info.main}`
                }}
              >
                <Box sx={{ 
                  display: 'flex',
                  alignItems: 'center',
                  mb: 1
                }}>
                  <Box sx={{ 
                    fontSize: '1.5rem',
                    mr: 1.5
                  }}>
                    {feature.icon}
                  </Box>
                  <Typography variant="h6" sx={{ 
                    fontSize: '0.9rem',
                    fontWeight: 600,
                    fontFamily: "'IBM Plex Mono', monospace"
                  }}>
                    {feature.title}
                  </Typography>
                </Box>
                <Typography variant="body2" sx={{ 
                  fontSize: '0.8rem',
                  color: theme.palette.text.secondary
                }}>
                  {feature.description}
                </Typography>
              </MotionBox>
            ))}
          </MotionBox>
        </Box>
      </Box>
    </Slide>
  );
};

export default DefenseUserExperience;
